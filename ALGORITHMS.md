# Алгоритмы генерации и решения лабиринтов

## Содержание
- [Алгоритмы генерации](#алгоритмы-генерации)
  - [DFS](#dfs-depth-first-search)
  - [Prim True](#prim-true)
  - [Prim Simplified](#prim-simplified)
  - [Prim Modified](#prim-modified)
- [Алгоритмы решения](#алгоритмы-решения)
  - [Dijkstra](#dijkstra)
  - [A*](#astar)

## Алгоритмы генерации

### DFS (Depth-First Search)

**Описание:** Рекурсивный поиск в глубину — самый простой и быстрый алгоритм генерации.

**Сложность:**
- Время: O(W × H)
- Память: O(W × H) — стек рекурсии

**Шаги:**
1. Заполнить сетку стенами
2. Выбрать случайную ячейку как стартовую
3. Пометить её как проход (посещённая)
4. Рекурсивно обходить соседей:
   - Выбрать случайного непосещённого соседа
   - Убрать стену между текущей и выбранной ячейкой
   - Рекурсивно обходить новую ячейку
   - Вернуться и попробовать других соседей

**Результат:** Лабиринт с длинными коридорами и множеством мёртвых концов.

**Пример вывода (10x10):**
```
#####################
#           #       #
# # # # # # # # # # #
# # # # # # # # # # #
# # # # # # # # # # #
# # # # # # # # # # #
# # # # # # # # # # #
# # # # # # # # # # #
# # # # # # # # # # #
#                   #
#####################
```

**Достоинства:**
- Очень быстро генерирует
- Простая реализация
- Низкое потребление памяти

**Недостатки:**
- Много длинных коридоров
- Меньше веток, чем у Prim

### Prim True

**Описание:** Классический алгоритм Прима для минимального остовного дерева, адаптированный для лабиринтов.

**Сложность:**
- Время: O(W × H × log(W × H)) с приоритетной очередью
- Память: O(W × H)

**Шаги:**
1. Заполнить сетку стенами
2. Выбрать случайную ячейку как стартовую (добавить в множество проходов)
3. Добавить все стены этой ячейки в список
4. Пока список не пуст:
   - Выбрать случайную стену из списка
   - Если стена разделяет проход и стену:
     - Сделать эту ячейку проходом
     - Добавить её соседние стены в список
   - Если оба конца стены уже проходы — пропустить

**Результат:** Более сбалансированный лабиринт с разветвлениями.

**Пример вывода (10x10):**
```
#####################
#   # #   # # #   # #
# # # # # # # # # # #
#   #   #   # #   # #
# # # # # # # # # # #
#   # #   #   #   # #
# # # # # # # # # # #
#       #   #   # # #
# # # # # # # # # # #
#   #       #       #
#####################
```

**Достоинства:**
- Лучше распределение маршрутов
- Более интересные для прохождения лабиринты
- Классический алгоритм, хорошо изучен

**Недостатки:**
- Медленнее DFS
- Требует приоритетную очередь

### Prim Simplified

**Описание:** Упрощённая версия Прима, использующая обычный список вместо приоритетной очереди.

**Сложность:**
- Время: O(W × H) в среднем
- Память: O(W × H)

**Отличия от Prim True:**
- Выбирает случайную стену из списка (O(1)) вместо извлечения минимума (O(log n))
- Проще реализация
- Немного быстрее на практике

**Результат:** Практически идентичен Prim True, но генерируется быстрее.

**Достоинства:**
- Хороший баланс скорости и качества
- Простая реализация
- Не требует сложных структур данных

**Недостатки:**
- Случайный выбор может дать менее "красивые" лабиринты
- Теряется оптимальность

### Prim Modified

**Описание:** Модифицированная версия Прима с использованием соседних ячеек вместо стен.

**Сложность:**
- Время: O(W × H)
- Память: O(W × H)

**Отличие:**
- Работает со списком соседних ячеек, а не со списком стен
- Более непосредственный подход

**Результат:** Похож на Prim Simplified.

**Когда использовать:**
- DFS — когда нужна максимальная скорость
- Prim Simplified — оптимальное соотношение скорости и качества
- Prim True — когда важна теоретическая корректность
- Prim Modified — альтернатива Prim для разных сценариев

## Алгоритмы решения

### Dijkstra

**Описание:** Алгоритм поиска кратчайшего пути с неотрицательными весами рёбер. В лабиринте все рёбра имеют вес 1.

**Сложность:**
- Время: O((W × H) × log(W × H)) с приоритетной очередью
- Память: O(W × H)

**Как работает:**
1. Инициализировать дистанции: стартовая = 0, остальные = ∞
2. Использовать приоритетную очередь для выбора ячейки с минимальной дистанцией
3. Для каждой ячейки:
   - Пометить как посещённую
   - Обновить дистанции соседей (релаксация)
   - Если сосед непроходим или посещён — пропустить
4. Восстановить путь по таблице `previous`

**Гарантия:** Всегда находит кратчайший путь, если он существует.

**Время выполнения (10x10 лабиринт):** ~5-20ms

**Когда использовать:**
- Когда нужен кратчайший путь
- Когда веса рёбер положительны
- Когда производительность важнее скорости поиска

### A* (A-Star)

**Описание:** Эвристический поиск, который комбинирует реальную дистанцию (как Dijkstra) и эвристику (прогноз до цели).

**Сложность:**
- Время: O((W × H) × log(W × H)) в худшем случае, но часто быстрее практически
- Память: O(W × H)

**Формула оценки:**
```
f(n) = g(n) + h(n)
где:
  g(n) = расстояние от старта до ячейки n
  h(n) = манхэттенская дистанция от n до цели
  f(n) = общая оценка
```

**Манхэттенская дистанция:**
```
h(n) = |n.x - goal.x| + |n.y - goal.y|
```

**Как работает:**
1. Инициализировать f-оценки: стартовая = h(start)
2. Использовать набор `openSet` для кандидатов
3. Выбирать ячейку с минимальной f-оценкой
4. Для каждого соседа:
   - Если новый g-путь лучше — обновить
   - Добавить в openSet
5. Восстановить путь

**Гарантия:** Находит кратчайший путь для положительных весов и допустимой эвристики.

**Манхэттенская дистанция допустима для 4-соседства (вверх, вниз, влево, вправо).**

**Время выполнения (10x10 лабиринт):** ~2-10ms

**Вишенка на торте:** A* обычно посещает меньше ячеек, чем Dijkstra, благодаря эвристике.

**Когда использовать:**
- Когда нужен кратчайший путь И скорость
- Когда есть явная цель
- Когда веса однородны (все = 1)
- Для игр, навигации, планирования маршрутов

## Сравнение

| Характеристика | DFS | Prim True | Prim Simplified | Prim Modified |
|---|---|---|---|---|
| Скорость генерации | ⚡⚡⚡ | ⚡⚡ | ⚡⚡⚡ | ⚡⚡⚡ |
| Распределение маршрутов | ❌ | ✅ | ✅ | ✅ |
| Сложность кода | ⭐ | ⭐⭐ | ⭐⭐ | ⭐⭐ |
| Интересность лабиринта | ⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |

| Характеристика | Dijkstra | A* |
|---|---|---|
| Скорость решения | ⚡⚡ | ⚡⚡⚡ |
| Гарантированно кратчайший | ✅ | ✅ |
| Эвристика | ❌ | ✅ |
| Сложность кода | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| Практическая скорость | ~15ms | ~5ms (10x10) |

## Рекомендации по использованию

**Для быстрого прототипирования:** DFS + Dijkstra

**Для красивых лабиринтов:** Prim Simplified + A*

**Для оптимальности:** Prim True + A*

**Для компромисса:** Prim Simplified + Dijkstra
